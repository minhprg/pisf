sub classifyNBC {
	
}

sub learnNBC {
	@vocab;
	@ham_scores;
	@spam_scores;
	
	for ($i = 0; $i < 2; $i++) {
		$docs = 50;
		$PROB_V = 50 / 100;
		$text_j = ; # get all thresholds
		$n = ; # number of distinct thresholds
		for ($j = 0; $j < $vocab) {
			$n_k = ; # number of time word K occurs in $text_j
			$PROB_W = ($n_k + 1) / ($n + scalar(grep $_ @vocab));
			if ($j == 0) {
				# save to ...
			}
			else {
				# save to ...
			}
		}
	}
}

1;